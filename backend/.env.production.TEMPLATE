# ===================================
# PRODUCTION ENVIRONMENT VARIABLES
# ===================================
# WICHTIG: Kopiere diese Datei und benenne sie um zu .env.production
# Dann fülle ALLE Werte mit deinen echten Production Credentials!

# Server
NODE_ENV=production
PORT=3000

# Frontend URL (Cloudflare Pages - wird nach Deployment bekannt)
FRONTEND_URL=https://verdrehtewelt.pages.dev

# MongoDB Atlas (Production Database)
# Ersetze <username>, <password> und <cluster-url> mit deinen echten Werten!
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/verdrehte-welt?retryWrites=true&w=majority

# PayPal LIVE Credentials
# Hole diese von: https://developer.paypal.com → My Apps & Credentials → LIVE
PAYPAL_CLIENT_ID=<deine-live-client-id>
PAYPAL_CLIENT_SECRET=<dein-live-secret>
PAYPAL_MODE=live
PAYPAL_WEBHOOK_ID=<webhook-id-nach-webhook-setup>

# JWT Secret (WICHTIG: Generiere einen neuen!)
# Generiere mit: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<generiere-einen-neuen-secret-key>
JWT_EXPIRY=365d

# Email Service (Resend)
# Hole API Key von: https://resend.com → API Keys
MAIL_API_KEY=re_<dein-resend-api-key>
MAIL_FROM=noreply@verdrehtewelt.de

# Cloudinary (Optional - für QR Code Storage)
# Falls du Cloudinary nutzen willst: https://cloudinary.com
CLOUDINARY_CLOUD_NAME=<optional>
CLOUDINARY_API_KEY=<optional>
CLOUDINARY_API_SECRET=<optional>

# Admin Password (Ändere das!)
ADMIN_PASSWORD=verdrehtewelt2025

# Platform Fee (5%)
PLATFORM_FEE_PERCENTAGE=5

# ===================================
# ANLEITUNG ZUM AUSFÜLLEN
# ===================================

# 1. MONGODB_URI:
#    - Gehe zu https://www.mongodb.com/cloud/atlas
#    - Erstelle kostenlosen Cluster (M0)
#    - Database Access → User erstellen
#    - Network Access → 0.0.0.0/0 erlauben
#    - Connect → Connection String kopieren
#    - <password> durch dein Passwort ersetzen

# 2. PAYPAL_CLIENT_ID & SECRET:
#    - Gehe zu https://developer.paypal.com
#    - Dashboard → My Apps & Credentials
#    - Wechsel zu "Live" (oben rechts)
#    - Create App → App Name: "Verdrehte Welt Ticketing"
#    - Client ID und Secret kopieren

# 3. PAYPAL_WEBHOOK_ID:
#    - PayPal Dashboard → Webhooks → Add Webhook
#    - URL: https://verdrehte-welt-backend.onrender.com/api/v1/payments/paypal/webhook
#    - Events: Payment capture completed, refunded, denied
#    - Webhook ID kopieren

# 4. JWT_SECRET:
#    - Terminal öffnen
#    - Ausführen: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#    - Output hier einfügen

# 5. MAIL_API_KEY:
#    - Gehe zu https://resend.com
#    - Sign Up (kostenlos)
#    - API Keys → Create API Key
#    - Key kopieren (nur einmal sichtbar!)

# 6. FRONTEND_URL:
#    - Wird nach Cloudflare Pages Deployment bekannt
#    - Format: https://verdrehtewelt.pages.dev
#    - Oder deine Custom Domain: https://verdrehtewelt.de
